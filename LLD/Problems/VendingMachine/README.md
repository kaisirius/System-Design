should have done something similar to like game LLDs, should have added a new class of ItemShelf having a code and item, this way we would have decoupled the "code" information from item to other class. Then inventory class would have list of shelfs not hashmap and no need of manager then. 